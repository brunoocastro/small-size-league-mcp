{
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "MCP health check",
        "description": "Checking if the app responds to the /health endpoint",
        "path": "/health",
        "attempts": 3
      }
    ]
  }
}